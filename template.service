[Unit]
Description={{ name }} Service
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=always
RestartSec=1
User={{ user }}
Group={{ group }}
ExecStart={{ exec }}

[Install]
WantedBy=multi-user.target